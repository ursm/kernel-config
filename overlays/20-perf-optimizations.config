#
# Opinionated performance optimizations (safe, broad benefits)
#

# Prefer performance over size in compiler
CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE=y

# Faster kernel image decompression
CONFIG_KERNEL_ZSTD=y

# Faster module load (compression with Zstd)
CONFIG_MODULE_COMPRESS=y
CONFIG_MODULE_COMPRESS_ZSTD=y

# Tickless idle: reduce unnecessary wakeups when idle
CONFIG_NO_HZ_IDLE=y

# Use native CPU optimizations on this machine (x86 only)
# Note: reduces portability of the resulting kernel image
CONFIG_X86_NATIVE_CPU=y
